= simple_form_for @period, :html => { :class => 'form-horizontal' } do |f|
  = f.input :number
  = f.input :kind
  = f.input :course_id, collection: Course.all
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), periods_path, :class => 'btn'
